Проанализируй лабораторные показатели и верни интерпретацию для каждого. Верни по одной строке на каждый тест, в том же порядке, без нумерации и заголовков.

Демография пациента: {{ sex_age_str }}
Название исследования: {{ exam_name }}

{% for item in test_items %}
=== Показатель {{ loop.index }} (тип: {{ item.bucket_type }}) ===
{{ item.test_json }}
{% endfor %}

Правила для строки:
- Начни сразу с названия показателя.
- Формат: <Название> (<единицы>): <значение> (норма: <описание нормы>).
- Не давай диагнозов, прогнозов и рекомендаций.
- Кратко поясни возможный смысл отклонения или что отражает показатель.
- Верни ровно по одной строке на каждый тест.

{% if has_abnormal %}Для показателей с отклонением (abnormal): после значения укажи норму и кратко поясни возможный смысл отклонения.
{% endif %}
{% if has_normal %}Для показателей в норме (normal): после значения укажи норму и кратко поясни, что отражает показатель.
{% endif %}
{% if has_lab_comment %}Для показателей с лабораторной шкалой (lab_comment): приведи шкалу дословно из reference.description, укажи положение значения.
{% endif %}
{% if has_no_ref %}Для показателей без референса (no_reference): кратко поясни, что отражает показатель; интерпретация зависит от клинического контекста.
{% endif %}

Верни ровно {{ test_items | length }} строк — по одной на каждый показатель, в том же порядке.

